# Database (Supabase or Railway Postgres)
# Use "Session pooler" in Supabase Connect (port 6543) to avoid IPv6 issues.
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Client URL (for payment links and Transak redirect). Use https://kemispay.com in production; http://localhost:5000 for local dev.
CLIENT_URL=https://kemispay.com

# Transak (https://docs.transak.com)
TRANSAK_API_KEY=your_transak_api_key
TRANSAK_ACCESS_TOKEN=your_transak_access_token
TRANSAK_BASE_URL=https://global-stg.transak.com

# Custody wallet - USDC from customer payments lands here.
# Must be a valid Ethereum address (0x + 40 hex chars).
KEMISPAY_CUSTODY_WALLET_ADDRESS=0xYourEthereumAddressHere

# Withdrawal tiers (optional)
AUTO_WITHDRAWAL_LIMIT_USD=500
WITHDRAWAL_DAILY_CAP_USD=500

# Supabase Storage (KYC documents)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: AWS SES (email)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
